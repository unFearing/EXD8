{
  "name": "exd8-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/src/functions/**/*.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "validate:foam-mechs": "node dist/scripts/validateFoamMechs.js",
    "sync:foam-to-cosmos": "node dist/scripts/syncFoamToCosmos.js",
    "sync:cosmos-to-foam": "node dist/scripts/syncCosmosToFoam.js",
    "sync:repository-index": "node dist/scripts/generateFoamRepositoryIndex.js",
    "sync:all": "npm run sync:foam-to-cosmos && npm run sync:repository-index && npm run sync:cosmos-to-foam",
    "watch:sync": "node dist/scripts/watchSync.js",
    "export:mechs-csv": "node dist/scripts/exportMechsToCsv.js",
    "prebuild": "npm run sync:all",
    "prestart": "npm run build",
    "start": "func start"
  },
  "dependencies": {
    "@azure/cosmos": "^4.3.0",
    "@azure/functions": "^4.7.3",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vitest": "^2.1.8"
  }
}
